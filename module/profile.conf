# Pixel device profile
profile=pixel9proxl

# Bootloop-safe settings (recommended defaults)
# file_hide=1     -> hide emulator dev files (safe paths only, no /system)
# late_file_hide=1 -> file hide runs in service.sh AFTER boot (smoother)
file_hide=1
late_file_hide=1

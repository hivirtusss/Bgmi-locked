# Match your AVD exactly:
#   pixel9   = Pixel 9 AVD (tokay) — default
#   pixel9a  = Pixel 9a AVD (tegu)
#   pixel9proxl = Pixel 9 Pro XL (komodo)
#   pixel6a | pixel7
profile=pixel9

# boot_apply=0 → props ONLY on Action tap (no crash). Set 1 to apply on boot.
boot_apply=0

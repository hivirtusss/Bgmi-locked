# Minimal early props — safe subset only (FreeRecharge did not use system.prop)
# Full props applied via post-fs-data.sh resetprop (safer on emulators)

ro.kernel.qemu=0
ro.boot.qemu=0
ro.build.type=user
ro.build.tags=release-keys
ro.debuggable=0
ro.secure=1
